# 從 `lnrm2.stan` 推導出來的結果

**這份文件的內容都不是程式碼字面寫的**，是從程式碼推導出來的。
程式碼實際寫了什麼，見 `lnrm2_stan_explained.md`。
數學形式見 `lnrm2_math.md`。

每一節的格式都是：**先貼出涉及的程式碼**，再推導，再用數字驗證。

以下用 `d_n` 代表那個重複出現的項：

$$d_n = \alpha \cdot \texttt{intensity[n]} + \alpha_2 \cdot \texttt{square\_intensity[n]}$$

---

## A. 為什麼 `z[1,tr]` 變小代表「反應變快」

### 涉及的程式碼

`lnrm2.stan:23-24`

```stan
z[1,tr] = mu - alpha * intensity[tr] - alpha2 * square_intensity[tr];
z[2,tr] = mu + alpha * intensity[tr] + alpha2 * square_intensity[tr];
```

`lnrm2.stan:38`

```stan
target += lognormal_lpdf(rt[tr] - psi | z[1,tr], varZ);
                                        ^^^^^^^
                                        z 出現在這裡
```

### 問題在哪

`z` **不是時間**。它是被放進 `lognormal_lpdf` 第一個分布參數位置的一個數。
所以「`z` 變小」和「反應變快」之間需要一座橋。

```
   z[1,tr] 變小   ---- ??? ----   反應時間變短
```

### 推導

`lognormal_lpdf(y | z, varZ)` 所描述的分布，等價於這樣產生一個值：

```
   抽一個常態隨機數 X，平均值 = z，標準差 = varZ
              |
              v
        y = exp(X)
```

程式碼餵進去的是 `rt[tr] - psi`，所以反推回來：

$$\text{reaction time} = \psi + \exp(X), \qquad X \sim \mathcal{N}(z,\; s^2)$$

（$s$ 就是程式碼裡的 `varZ`。）

`exp()` 是遞增函數：輸入越大，輸出越大。所以

```
   z 小  ->  X 平均小  ->  exp(X) 小  ->  reaction time 短
```

到這裡只證明了「**平均**變快」。但實際上成立的是更強的版本：

$$\exp(X) = \exp(z + \varepsilon) = \underbrace{\exp(z)}_{\text{只跟 } z \text{ 有關}} \cdot \underbrace{\exp(\varepsilon)}_{\text{跟 } z \text{ 完全無關}}, \qquad \varepsilon \sim \mathcal{N}(0,\; s^2)$$

`z` 變成一個純粹的**乘法倍率**。所以改變 `z` 不是把分布往左推，
而是把整條分布**等比例縮放**。

```
   z = 1.5    |----[====]--------|     快       中       慢
                                       2.08    4.47    9.66

   z = 1.0  |--[===]-----|               1.26    2.71    5.84

                                   每個位置都乘上約 0.61
```

### 數字驗證（varZ = 0.6）

| | 快的（前 10%） | 中位數 | 慢的（後 10%） |
|---|---|---|---|
| z = 1.5 | 2.08 | 4.47 | 9.66 |
| z = 1.0 | 1.26 | 2.71 | 5.84 |
| 比值 | 0.61 | 0.61 | 0.60 |

三個位置的比值相同，代表是整條分布縮放，不只是平均值移動。

---

## B. `d_n` 等於 0 時，正確率恰好是 50%

### 涉及的程式碼

`lnrm2.stan:23-24`（同上）

### 推導

`d_n` 等於 0 時，把它代入那兩行：

$$z_{1n} = \mu - 0 = \mu, \qquad z_{2n} = \mu + 0 = \mu$$

兩列完全相同。

再看 `lnrm2.stan:38-43`，兩個分支用的分布參數變成一模一樣：

```
   c_n = 1:   f( . ; mu )  *  S( . ; mu )
   c_n = 0:   f( . ; mu )  *  S( . ; mu )
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^
                    兩者完全相同
```

兩個條件完全相同的東西比「誰先發生」，機率必然各半——
否則無法說明憑什麼是這一個而不是那一個。

**這個結論只用到「兩者相同」**，連對數常態這個假設都不需要，
換成任何連續分布都成立。

### 數字驗證

模擬 200 萬次，得到 `0.50036`。

---

## C. 正確率可以直接算出來

### 涉及的程式碼

`lnrm2.stan:23-24` 加 `lnrm2.stan:38-43`。
關鍵是**兩列共用同一個 `varZ`**（兩個分支的第二個分布參數都是 `varZ`）。

### 推導

設兩個完成時間為：

$$T_1 = \psi + \exp(X_1), \quad X_1 \sim \mathcal{N}(z_{1n},\, s^2)$$
$$T_2 = \psi + \exp(X_2), \quad X_2 \sim \mathcal{N}(z_{2n},\, s^2)$$

答對等價於 `T_1 < T_2`，也等價於 `X_1 < X_2`（因為 psi 和 exp 都是遞增的）。
兩個獨立常態相減仍是常態：

$$X_1 - X_2 \sim \mathcal{N}\big(z_{1n} - z_{2n},\; 2 s^2\big)$$

而

$$z_{1n} - z_{2n} = (\mu - d_n) - (\mu + d_n) = -2 d_n$$

所以

$$P(\text{correct}) = P(X_1 - X_2 < 0)
= \Phi\!\left(\frac{2 d_n}{s\sqrt{2}}\right)
= \Phi\!\left(\frac{\sqrt{2}\, d_n}{s}\right)$$

`Phi` 是標準常態的累積分布函數，唯一重要的性質是**輸入越大、輸出越大**。
所以整個公式只要看兩件事：

$$P(\text{correct}) = \Phi\!\left(\frac{\sqrt{2}\,d_n}{s}\right)$$

```
              分子 d_n 越大  ->  正確率越高
              分母 s 越大    ->  正確率越低   (s = varZ, 雜訊)
```

### 數字驗證（varZ = 0.6）

| d_n | 正確率 |
|---|---|
| 0 | 50% |
| 0.3 | 76% |
| 1.0 | 99% |

**推論：正確率只取決於 `d_n / varZ` 這個比值，與 `mu` 和 `psi` 完全無關。**
`mu` 和 `psi` 只影響反應時間落在哪裡。

---

## D. 二次項讓曲線可以彎

### 涉及的程式碼

`lnrm2.stan:10`

```stan
square_intensity = square(intensity);
```

`lnrm2.stan:23-24`（同上）

### 推導

把 `square_intensity[n]` 換回 `intensity[n]^2`：

$$d_n = \alpha x_n + \alpha_2 x_n^2$$

這是 `x_n` 的二次式，畫出來是拋物線。

```
   alpha2 = 0            alpha2 < 0
                                ___
        /                    __/   \__
       /                    /         \
      /                    /           \
     /                    /             \
   直線                  拋物線（會飽和然後下降）
```

**這一條幾乎沒有推導成分**，只是把變數名換回原式而已。

---

## E. 「先完成的那個決定 RT」——這件事沒有寫在程式碼裡

### 涉及的程式碼

`lnrm2.stan:38-39`

```stan
target += lognormal_lpdf (rt[tr] - psi | z[1,tr], varZ);
target += lognormal_lccdf(rt[tr] - psi | z[2,tr], varZ);
```

`lnrm2.stan:42-43`（else 分支，兩列對調）

### 先講清楚

**`lnrm2.stan` 全檔沒有任何取最小值的運算。**
唯一出現 "min" 字樣的是第 5 行的 `minRT` 和第 17 行 `psi` 的上界，那是另一回事。

所以如果你在程式碼裡找「取 min」那一步，**找不到是正常的，它不存在**。

### 這個結構藏在哪

「賽跑」的意思完全隱含在那兩個函式的**配對**裡：

```
   lognormal_lpdf ( ... | z[1,tr], varZ )     機率密度
            +                                     (log 相加
   lognormal_lccdf( ... | z[2,tr], varZ )     存活函數      = 原尺度相乘)
```

R 端 `adaptiveSFT_functions.R:61-72` 的 `dlognormalrace()` 是同一個結構：

```r
g <- dlnorm(x-psi, mu[m], sigma[m], log=TRUE)                    # 機率密度
G <- G + plnorm(x-psi, mu[i], sigma[i], lower=FALSE, log=TRUE)   # 存活函數
rval <- exp(g + G)
```

同樣沒有取最小值的運算。

### 未確認

這個讀法（「密度乘存活函數」代表「先完成的那個決定 RT」）**無法從程式碼確認**。
見 `lnrm2_stan_explained.md` 第 11.1 節的待確認問題。

---

## F. RT 的方向不是自動的

**這一節的前提是 E 的讀法成立。若 E 不成立，本節作廢。**

### 涉及的程式碼

`lnrm2.stan:23-24`。`d_n` 變大時：

```
   z[1,tr] = mu - d_n     -->  變小  -->  "答對" 的完成時間變短
   z[2,tr] = mu + d_n     -->  變大  -->  "答錯" 的完成時間變長
```

用數學寫是 $z_{1n} = \mu - d_n$ 與 $z_{2n} = \mu + d_n$。

### 問題

如果 RT 是兩者中先完成的那一個，那這兩個變化**方向相反**：

```
   "答對" 變快  ---->  拉低 RT
                                  兩股力量互相拉扯
   "答錯" 變慢  ---->  推高 RT
```

為什麼「答錯變慢」會推高 RT？**因為在那些「答錯先完成」的試次上，
它變慢就代表該次的 RT 變長。**

### 四個試次的示範

假設 `d_n` 變大時，答對那邊快 1、答錯那邊慢 1。

改變前：

```
   試次   答對    答錯   誰先   RT
    1     3.0     4.0    對    3.0
    2     5.0     2.0    錯    2.0
    3     2.0     6.0    對    2.0
    4     4.0     3.0    錯    3.0
                              ----
                         平均  2.50    正確率 2/4
```

改變後：

```
   試次   答對    答錯   誰先   RT     變化
    1     2.0     5.0    對    2.0    3.0 -> 2.0   變快
    2     4.0     3.0    錯    3.0    2.0 -> 3.0   變慢  <<<
    3     1.0     7.0    對    1.0    2.0 -> 1.0   變快
    4     3.0     4.0    對    3.0    3.0 -> 3.0   不變
                              ----
                         平均  2.25    正確率 3/4
```

**試次 2 是關鍵。** 那一次是「答錯」先完成（2.0 比 5.0 早），
而它變慢成 3.0，所以該次的 RT 從 2.0 變成 3.0 ——**變慢了**。

### 結論

```
   "答對" 先完成的試次  ->  RT 變快
   "答錯" 先完成的試次  ->  RT 變慢
                            ^^^^^^
                     這個效果真實存在，不能忽略
```

哪一邊主導，取決於兩種試次各佔多少，**必須實際計算**。

### 數字驗證（varZ = 0.6）

| d_n | 答錯的比例 | 全部 RT 中位數 | 答對的 RT | 答錯的 RT |
|---|---|---|---|---|
| 0.0 | 50% | 3.353 | 3.350 | 3.357 |
| 0.3 | 24% | 3.111 | 2.946 | 3.639 |
| 1.0 | 1% | 1.770 | 1.760 | 4.076 |
| 1.5 | 0.02% | 1.119 | 1.119 | 4.144 |

答錯的 RT 確實一路上升（3.357 到 4.144），就是試次 2 那個效果。
但這種試次的比例掉到 0.02%，拉不動整體平均，所以「全部 RT」仍然單調下降。

**結論：「刺激越明顯反應越快」對整體與答對的試次成立，對答錯的試次相反。**
